// Vendored from caelestia-dots/shell — components/CAnim.qml. Aurora build; local changes tracked in git.
import QtQuick
import Caelestia.Config

ColorAnimation {
    duration: Tokens.anim.durations.expressiveSlowEffects
    easing: Tokens.anim.expressiveSlowEffects
}
