// Vendored from caelestia-dots/shell — components/StyledClippingRect.qml. Aurora build; local changes tracked in git.
import QtQuick
import Quickshell.Widgets

ClippingRectangle {
    id: root

    color: "transparent"

    Behavior on color {
        CAnim {}
    }
}
