// Vendored from caelestia-dots/shell — components/StyledRect.qml. Aurora build; local changes tracked in git.
import QtQuick

Rectangle {
    id: root

    color: "transparent"

    Behavior on color {
        CAnim {}
    }
}
